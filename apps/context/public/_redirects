# Sitemap files - serve directly (must come before SPA fallback)
/sitemap.xml       /sitemap.xml       200
/sitemap-*.xml     /sitemap-*.xml     200
/sitemap.xsl       /sitemap.xsl       200

# SPA fallback - serve __spa-fallback.html for client-side routing
# This allows React Router to handle dynamic routes like /entry/:id
/*                 /__spa-fallback.html   200
